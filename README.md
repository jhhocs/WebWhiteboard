# WebWhiteboard

Run client:

1. Navigate into client directory
2. Run ```npm start```

Run server:

1. Navigate into server directory
2. Run ```npm start```

Deploying client to GitHub pages:

1. Navigate into client directory
2. Run ```npm deploy```